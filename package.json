{
  "name": "vip-ride",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "pnpm --filter @vip-ride/web dev",
    "dev:api": "pnpm --filter @vip-ride/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.6.2"
  }
}
