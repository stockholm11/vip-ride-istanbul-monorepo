{
  "name": "@vip-ride/domain",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && tscpaths -p tsconfig.json -s src -o dist"
  },
  "devDependencies": {
    "tscpaths": "^0.0.9"
  }
}

